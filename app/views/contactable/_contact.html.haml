-#  Copyright (c) 2012-2021, German Contingent for the Worldscoutjamboree 2023. This file is part of
-#  hitobito_jamda_2023 and licensed under the Affero General Public License version 3
-#  or later. See the COPYING file at the top-level directory or at
-#  https://github.com/mwppoe/jamda_2023.

%article.col-lg
  %article.span5
    = render 'contact_data', person: entry, only_public: cannot?(:show_details, entry)
 
    - if @person.role_wish == "Patrullenbetreuer*in" && @person.unit_keys.present?
      %h3= "Durch Unit Tags zugeordnete Teilnehmer*innen"
      = participant_list(@person)
      %br
    - if @person.role_wish == "Teilnehmer*in" && @person.unit_keys.present?
      %h3= "Wunsch Unit Leiter*in"
      = find_unit_leader(@person)


  %aside.span4.offset1
    - if can?(:show_details, entry)
      %div
        = render_attrs(entry, :birthday, :gender)

        = render_extensions :details, locals: { show_full: can?(:show_full, entry) }

        = render_attrs(entry, :created_info, :updated_info)
    

    = render_attrs(entry, :phone)

    = render_attrs(entry, :status,
                          :role_wish)
    = render_attrs(entry, :passport_nationality,
                          :passport_number,
                          :passport_austria,
                          :passport_valid)
    = render_attrs(entry, :airport)
    = render_attrs(entry, :languages_spoken,
                          :shirt_size,
                          :skills)
    = render_attrs(entry, :add_good_1,
                          :add_good_2,
                          :add_good_3,    
                          :add_good_4)                                                        
    = render_extensions :show_left

    
%article.col-lg
